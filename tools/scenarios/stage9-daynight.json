[
  { "wait": 3000 },
  { "eval": "(() => { const A = game.atmosphere, r = game.engine.renderer; return { hora: A.clock, elevSol: +A.sunElevation.toFixed(2), exposicao: +r.toneMappingExposure.toFixed(2), luzPrincipal: +A.keyIntensity.toFixed(2), corLuz: A.keyColor.getHexString(), nevoa: game.engine.scene.fog.color.getHexString(), hemi: +A.hemi.intensity.toFixed(2), noite: +A.night.toFixed(2), esperado: { corLuz: 'ffa860', nevoa: '7e6b63', luzPrincipal: 6, hemi: 3.2, exposicao: 1.1 } }; })()", "label": "início = fim de tarde original?" },
  { "eval": "(() => { const A = game.atmosphere; let prev = null, maxJump = 0, at = null, maxExp = 0; const lum = () => A.keyIntensity * Math.max(A.lightDir.y, 0) + A.hemi.intensity * 0.3; for (let h = 0; h <= 24.0001; h += 0.02) { A.setHour(h); const v = lum(); if (prev) { const j = Math.abs(v - prev.v); if (j > maxJump) { maxJump = j; at = h.toFixed(2); } maxExp = Math.max(maxExp, Math.abs(A.exposure - prev.e)); } prev = { v, e: A.exposure }; } A.setHour(17); return { maiorSaltoDeLuzEntrePassosDe1min: +maxJump.toFixed(3), naHora: at, maiorSaltoDeExposicao: +maxExp.toFixed(3) }; })()", "label": "continuidade da luz ao longo de 24 h" },
  { "eval": "(() => { const A = game.atmosphere, cam = game.engine.camera, p = game.player.position; A.setHour(17); let t = 0, sunset = null, dark = null, dawn = null; const dt = 0.5; for (let i = 0; i < 20000; i++) { A.update(p, cam, dt); t += dt; if (sunset === null && A.sunElevation < 0) sunset = t; if (dark === null && A.sunElevation < -8) dark = t; if (sunset !== null && dawn === null && A.sunElevation > 0 && A.hour < 12) dawn = t; if (t > 60 && Math.abs(A.hour - 17) < 0.008) break; } const out = { minutosAtePorDoSol: +(sunset / 60).toFixed(1), minutosAteNoiteFechada: +(dark / 60).toFixed(1), minutosDeNoite: +((dawn - sunset) / 60).toFixed(1), minutosCicloCompleto: +(t / 60).toFixed(1) }; A.setHour(17); return out; })()", "label": "duração das fases (tempo real)" },
  { "eval": "(() => { const g = game, p = g.player; p.position.set(-29, g.terrain.heightAt(-29, -28), -28); p.velocity.set(0, 0, 0); g.chunks.warmup(p.position); window.__at = (h, target, yawOffset = 0) => { const A = g.atmosphere; A.setHour(h); const d = target === 'lua' ? A.moonDir : target === 'sol' ? A.sunDir : null; if (d) { p.yaw = Math.atan2(-d.x, -d.z) + yawOffset; p.pitch = Math.min(Math.asin(d.y) * 0.8, 0.6) + 0.02; } else { p.yaw = 1.0 + yawOffset; p.pitch = 0.05; } return { hora: A.clock, elevSol: +A.sunElevation.toFixed(1), noite: +A.night.toFixed(2), exposicao: +A.exposure.toFixed(2), luz: +A.keyIntensity.toFixed(2) }; }; return 'clareira'; })()" },
  { "eval": "__at(17.0, 'sol', 0.5)", "label": "17:00" },
  { "wait": 3000 },
  { "shot": "s9-01-1700" },
  { "eval": "__at(18.05, 'sol')", "label": "pôr do sol" },
  { "wait": 3000 },
  { "shot": "s9-02-sunset" },
  { "eval": "__at(18.9, 'lua')", "label": "anoitecendo, lua nascendo" },
  { "wait": 3000 },
  { "shot": "s9-03-moonrise" },
  { "eval": "__at(23.5, 'lua')", "label": "noite: lua alta e estrelas" },
  { "wait": 3000 },
  { "shot": "s9-04-night-moon" },
  { "eval": "__at(23.5, 'free', 2.2)", "label": "noite olhando a mata (dá para ver?)" },
  { "wait": 3000 },
  { "shot": "s9-05-night-forest" },
  { "eval": "__at(5.9, 'sol')", "label": "amanhecer" },
  { "wait": 3000 },
  { "shot": "s9-06-dawn" },
  { "eval": "__at(12.0, 'free')", "label": "meio-dia" },
  { "wait": 3000 },
  { "shot": "s9-07-noon" },
  { "eval": "(() => { game.atmosphere.setHour(17); return game.atmosphere.clock; })()" }
]
