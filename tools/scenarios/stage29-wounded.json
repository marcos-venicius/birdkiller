[
  { "wait": 4000 },
  { "eval": "(() => { const g = game, p = g.player, w = g.weapon, inp = g.input, t = g.tools, d = g.dog; window.__tick = (n) => { for (let i = 0; i < n; i++) { p.update(1 / 30); w.update(1 / 30); t.update(1 / 30); g.birds.update(1 / 30); g.boars.update(1 / 30); g.deer.update(1 / 30); d.update(1 / 30); inp.endFrame(); } }; window.__herd = (M, n, R, id) => { for (const b of M.active) b.removable = true; __tick(1); const V = p.position.constructor; let S = null; for (let k = 0; k < 40 && !S; k++) { const a = Math.atan2(-Math.sin(p.yaw), -Math.cos(p.yaw)) + (k % 2 ? 1 : -1) * Math.ceil(k / 2) * 0.2; const x = p.position.x + Math.sin(a) * R, z = p.position.z + Math.cos(a) * R; if (g.chunks.isClear(x, z, 2.5) && g.terrain.lakes.dry(x, z, 3)) S = { x, z }; } const herd = []; for (let k = 0; k < n; k++) { const b = M.spawn(new V(S.x + k * 2, 0, S.z + (k % 2) * 2), Math.random() * 6.28, k ? 0.9 : 1.05, k); b.herdId = id; herd.push(b); } for (const b of herd) b.leader = herd[0]; return herd; }; window.__segDist = (q, path) => { let best = 1e9; for (let i = 1; i < path.length; i++) { const a = path[i - 1], b = path[i]; const vx = b.x - a.x, vz = b.z - a.z, L2 = vx * vx + vz * vz || 1e-9; const u = Math.max(0, Math.min(1, ((q.x - a.x) * vx + (q.z - a.z) * vz) / L2)); best = Math.min(best, Math.hypot(q.x - a.x - vx * u, q.z - a.z - vz * u)); } return best; }; g.blood.clear(); d.setMode('heel'); const herd = __herd(g.deer, 3, 60, 7001); window.__H = herd; return { bando: herd.length, distancia: +Math.hypot(herd[0].pos.x - p.position.x, herd[0].pos.z - p.position.z).toFixed(0) }; })()", "label": "bando de 3 veados a 60 m" },
  { "eval": "(() => { const g = game, p = g.player, H = __H, w = H[0]; const V = p.position.constructor; g.deer.hit(w, new V(0, 0, 1), false, p.position.clone()); const path = [{ x: w.pos.x, z: w.pos.z }]; const start = H.map((b) => ({ x: b.pos.x, z: b.pos.z })); const run = H.map(() => 0); const top = H.map(() => 0); let prev = H.map((b) => ({ x: b.pos.x, z: b.pos.z })); for (let i = 0; i < 30; i++) { __tick(15); H.forEach((b, k) => { run[k] += Math.hypot(b.pos.x - prev[k].x, b.pos.z - prev[k].z); prev[k] = { x: b.pos.x, z: b.pos.z }; top[k] = Math.max(top[k], b.speed); }); path.push({ x: w.pos.x, z: w.pos.z }); } window.__path = path; const drops = g.blood.drops; const V3 = new V(); let near = 0; for (let i = 0; i < drops; i++) { g.blood.position(i, V3); if (__segDist(V3, path) < 0.8) near++; } return { ferido: { estado: w.state, vida: w.health, velMax: +top[0].toFixed(2), andou15s: +run[0].toFixed(0), largouOLider: w.leader === null }, sadios: H.slice(1).map((b, k) => ({ velMax: +top[k + 1].toFixed(2), andou15s: +run[k + 1].toFixed(0) })), gotas: drops, gotasEmCimaDaTrilha: near }; })()", "label": "tiro na traseira: 15 s depois" },
  { "eval": "(() => { const g = game, p = g.player, w = __H[0]; g.deer.frozen = true; const x = w.pos.x + 9, z = w.pos.z + 7; p.position.set(x, g.terrain.heightAt(x, z), z); p.yaw = Math.atan2(-(w.pos.x - x), -(w.pos.z - z)); p.pitch = -0.12; p.update(1 / 30); return { estado: w.state, velocidade: +w.speed.toFixed(2) }; })()", "label": "de perto do ferido (bichos congelados para a captura)" },
  { "wait": 1500 },
  { "shot": "s29-01-veado-ferido" },
  { "eval": "(() => { const g = game, p = g.player, P = __path[0]; const x = P.x + 2.5, z = P.z + 1.5; p.position.set(x, g.terrain.heightAt(x, z), z); p.yaw = Math.atan2(-(P.x - x), -(P.z - z)); p.pitch = -0.75; p.update(1 / 30); return 'olhando o sangue no ponto do tiro'; })()" },
  { "wait": 1500 },
  { "shot": "s29-02-sangue" },
  { "eval": "(() => { const g = game, p = g.player, w = __H[0]; g.deer.frozen = false; const x = w.pos.x + 150, z = w.pos.z; p.position.set(x, g.terrain.heightAt(x, z), z); const antes = g.blood.drops; let i = 0; for (; i < 200 && w.state !== 'bed'; i++) __tick(15); const poça = g.blood.drops; __tick(3); return { estado: w.state, segundosAteDeitar: i / 2, gotasAntes: antes, gotasDepoisDeDeitar: g.blood.drops, poçaFeita: w.pooled }; })()", "label": "anda mancando e deita (jogador longe)" },
  { "eval": "(() => { const g = game, p = g.player, w = __H[0]; const x = w.pos.x + 20, z = w.pos.z; p.position.set(x, g.terrain.heightAt(x, z), z); p.velocity.set(0, 0, 0); __tick(6); const e1 = w.state; let top = 0; for (let i = 0; i < 6; i++) { __tick(15); top = Math.max(top, w.speed); } return { aoChegarA20m: e1, depois: w.state, velMaxFugindo: +top.toFixed(2), fugaNormalDoVeado: 12.5 }; })()", "label": "jogador chega perto: levanta e foge mancando" },
  { "eval": "(() => { const g = game, p = g.player, w = __H[0]; const V = p.position.constructor; const r = g.deer.hit(w, new V(0, 0, 1), false, p.position.clone()); __tick(30); return { segundoTiro: r, estado: w.state }; })()", "label": "segundo tiro (mesmo na traseira) mata" },
  { "eval": "(() => { const g = game, p = g.player, d = g.dog; const herd = __herd(g.deer, 2, 90, 7002); const V = p.position.constructor; g.deer.hit(herd[1], new V(0, 0, 1), false, p.position.clone()); window.__W = herd[1]; d.respawnNearPlayer(); d.setMode('deer'); let i = 0; for (; i < 260 && d.state !== 'point'; i++) { const dx = d.pos.x - p.position.x, dz = d.pos.z - p.position.z, dist = Math.hypot(dx, dz); if (dist > 10) { const s = Math.min(dist - 10, 2.25); const x = p.position.x + (dx / dist) * s, z = p.position.z + (dz / dist) * s; p.position.set(x, g.terrain.heightAt(x, z), z); } __tick(15); } const t = d.target; const r = { estado: d.state, segundos: i / 2, alvoEhOFerido: t === __W, alvoFerido: !!(t && t.wounded), estadoDoFerido: __W.state, aviso: document.querySelector('.hud-note').textContent }; d.setMode('heel'); return r; })()", "label": "cão farejando veado vai no ferido" },
  { "eval": "(() => { const g = game, p = g.player, B = g.birds; for (const b of B.active) b.removable = true; __tick(1); const sp = B.speciesList.find((s) => !s.water && !s.rare) ?? B.speciesList[0]; const V = p.position.constructor; const fx = -Math.sin(p.yaw), fz = -Math.cos(p.yaw); const x = p.position.x + fx * 14, z = p.position.z + fz * 14; const b = B.spawnFlying(sp, new V(x, g.terrain.heightAt(x, z) + 9, z), 0.5); window.__B = b; B.wound(b, new V(fx, 0, fz)); let t = 0; while (t < 10 && b.pos.y - g.terrain.heightAt(b.pos.x, b.pos.z) > sp.length * 0.25) { __tick(3); t += 0.1; } const land = +t.toFixed(1); let maxUp = 0; let hops = 0; let prevY = b.pos.y; for (let i = 0; i < 300; i++) { __tick(3); const up = b.pos.y - g.terrain.heightAt(b.pos.x, b.pos.z); maxUp = Math.max(maxUp, up); if (b.pos.y > prevY + 0.02) hops++; prevY = b.pos.y; } return { especie: sp.name, estado: b.state, segundosAteOChao: land, alturaMaximaEm30s: +maxUp.toFixed(2), comprimento: sp.length, subidas: hops }; })()", "label": "ave com a asa ferida: cai e fica no chão 30 s" },
  { "eval": "(() => { const g = game, p = g.player, b = __B; const x = b.pos.x + 5, z = b.pos.z; p.position.set(x, g.terrain.heightAt(x, z), z); p.velocity.set(0, 0, 0); const d0 = Math.hypot(b.pos.x - x, b.pos.z - z); __tick(90); const d1 = Math.hypot(b.pos.x - x, b.pos.z - z); return { estado: b.state, distanciaAntes: +d0.toFixed(1), depoisDe3s: +d1.toFixed(1) }; })()", "label": "jogador a 5 m: ela foge pulando" },
  { "eval": "(() => { const g = game, p = g.player, b = __B; const x = b.pos.x + 1.6, z = b.pos.z + 1.2; p.position.set(x, g.terrain.heightAt(x, z), z); p.yaw = Math.atan2(-(b.pos.x - x), -(b.pos.z - z)); p.pitch = -0.7; g.birds.frozen = true; p.update(1 / 30); return 'olhando a ave no chão'; })()" },
  { "wait": 1500 },
  { "shot": "s29-03-ave-no-chao" },
  { "eval": "(() => { const g = game, p = g.player, b = __B; g.birds.frozen = false; const V = p.position.constructor; g.birds.kill(b, new V(0, 0, 1)); __tick(60); return { estado: b.state }; })()", "label": "tiro no corpo mata a ave no chão" },
  { "eval": "(() => { const g = game, p = g.player, B = g.birds; const sp = B.speciesList.find((s) => !s.water && !s.rare) ?? B.speciesList[0]; const V = p.position.constructor; p.pitch = 0; p.update(1 / 30); const cam = g.engine.camera; cam.updateMatrixWorld(); const f = new V(); cam.getWorldDirection(f); const pos = cam.position.clone().addScaledVector(f, 15); const b = B.spawnFlying(sp, pos, Math.atan2(f.x, f.z) + Math.PI / 2); B.frozen = true; b.update(0.05, B); const aim = b.pos.clone().add(new V(0, -0.65 * sp.length, 0)).sub(cam.position).normalize(); g.hunting.shoot(cam.position.clone(), aim); for (let i = 0; i < 20; i++) g.hunting.update(1 / 60, cam.position); const s = g.hunting.lastShot; B.frozen = false; return { tiro: s && { tipo: s.kind, letal: s.lethal, dist: +s.distance.toFixed(1) }, estadoDaAve: b.state, aviso: document.querySelector('.hud-kill').textContent }; })()", "label": "tiro real de raspão na asa (pela Hunting)" },
  { "eval": "(() => { const g = game; const t0 = performance.now(); for (let i = 0; i < 200; i++) g.blood.drop(g.player.position.x + Math.random() * 20, g.player.position.z + Math.random() * 20); const ms = (performance.now() - t0) / 200; g.session.clear(); return { msPorGota: +ms.toFixed(3), gotasNoMundo: g.blood.drops }; })()", "label": "custo de uma gota" }
]
