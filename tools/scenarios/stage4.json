[
  { "wait": 2500 },
  { "eval": "(() => { window.__census = () => { const s = {}, sp = {}; for (const b of game.birds.active) { s[b.state] = (s[b.state] || 0) + 1; sp[b.species.id] = (sp[b.species.id] || 0) + 1; } return { total: game.birds.active.length, estados: s, especies: sp }; }; return __census(); })()", "label": "população inicial" },
  { "eval": "(() => { const g = game, p = g.player, cam = g.engine.camera; g.birds.frozen = true; const f = { x: -Math.sin(p.yaw), z: -Math.cos(p.yaw) }, r = { x: Math.cos(p.yaw), z: -Math.sin(p.yaw) }; const e = cam.position; const list = g.birds.speciesList; window.__show = []; const P = (dx, dy, dist) => new e.constructor(e.x + f.x * dist + r.x * dx, e.y + dy, e.z + f.z * dist + r.z * dx); list.forEach((sp, i) => { const off = (i - (list.length - 1) / 2) * 0.5; const a = g.birds.spawnPerched(sp, P(off, -0.35, 2.2), false, null); a.showcase(P(off, -0.3, 2.2), p.yaw + Math.PI / 2, 0, 1); const b = g.birds.spawnPerched(sp, P(off, 0.3, 2.2), false, null); b.showcase(P(off, 0.3, 2.2), p.yaw + 2.2, 0.9, 0); window.__show.push(a, b); }); return list.map(s => s.name); })()", "label": "vitrine" },
  { "wait": 2500 },
  { "shot": "s4-01-showcase" },
  { "eval": "(() => { const B = game.birds; for (const b of window.__show) { const i = B.active.indexOf(b); if (i >= 0) B.release(i); } B.frozen = false; return __census(); })()", "label": "vitrine removida" },
  { "eval": "(() => { const g = game, B = g.birds; const log = []; B.onSpawn = (b, init) => { const P = g.player.position; const dx = b.pos.x - P.x, dz = b.pos.z - P.z, d = Math.hypot(dx, dz); const fx = -Math.sin(g.player.yaw), fz = -Math.cos(g.player.yaw); log.push({ d: Math.round(d), ang: Math.round(Math.acos(Math.max(-1, Math.min(1, (dx * fx + dz * fz) / d))) * 57.3), st: b.state }); }; const t0 = performance.now(); for (let i = 0; i < 2700; i++) B.update(1 / 30); const ms = (performance.now() - t0) / 2700; let nan = 0, belowGround = 0; for (const b of B.active) { if (!Number.isFinite(b.pos.x + b.pos.y + b.pos.z)) nan++; if (b.pos.y < g.terrain.heightAt(b.pos.x, b.pos.z) - 0.05) belowGround++; } const near = log.filter(s => s.d < 150); window.__spawnLog = log; return { msPorQuadro: +ms.toFixed(3), ...__census(), nan, belowGround, spawns: log.length, menorAnguloSpawnPerto: near.length ? Math.min(...near.map(s => s.ang)) : null, menorDistancia: log.length ? Math.min(...log.map(s => s.d)) : null }; })()", "label": "simulação 90 s" },
  { "eval": "(() => { const B = game.birds; const ok = B.active.map(b => b.state === 'perched' ? +(b.pos.y - game.terrain.heightAt(b.pos.x, b.pos.z)).toFixed(1) : null).filter(v => v !== null); return { alturasPousados: ok }; })()", "label": "altura dos pousados (chão ≈ 0,05; copa ≈ 5–20)" },
  { "eval": "(() => { const g = game, B = g.birds, p = g.player; B.frozen = true; const e = g.engine.camera.position; let best = null, bd = 1e9; for (const b of B.active) { if (b.state !== 'perched') continue; const d = b.pos.distanceTo(e); if (d > 12 && d < 70 && d < bd) { bd = d; best = b; } } if (!best) return 'nenhum pousado'; const dx = best.pos.x - e.x, dy = best.pos.y - e.y, dz = best.pos.z - e.z; p.yaw = Math.atan2(-dx, -dz); p.pitch = Math.atan2(dy, Math.hypot(dx, dz)); g.input.buttonsPressed |= 4; for (let i = 0; i < 12; i++) { p.update(1 / 30); g.weapon.update(1 / 30); g.input.endFrame(); } return { especie: best.species.name, dist: +bd.toFixed(1), noChao: best.onGround }; })()", "label": "mirando num pousado" },
  { "wait": 2500 },
  { "shot": "s4-02-scope-perched" },
  { "eval": "(() => { const g = game, B = g.birds, p = g.player; const e = g.engine.camera.position; let best = null, bd = 1e9; for (const b of B.active) { if (b.state !== 'flying') continue; const d = b.pos.distanceTo(e); if (d > 15 && d < 90 && d < bd) { bd = d; best = b; } } if (!best) return 'nenhum voando'; const dx = best.pos.x - e.x, dy = best.pos.y - e.y, dz = best.pos.z - e.z; p.yaw = Math.atan2(-dx, -dz); p.pitch = Math.atan2(dy, Math.hypot(dx, dz)); for (let i = 0; i < 3; i++) { p.update(1 / 30); g.weapon.update(1 / 30); } return { especie: best.species.name, dist: +bd.toFixed(1) }; })()", "label": "mirando num voando" },
  { "wait": 2500 },
  { "shot": "s4-03-scope-flying" },
  { "eval": "(() => { const g = game; g.input.buttonsPressed |= 4; for (let i = 0; i < 12; i++) { g.player.update(1 / 30); g.weapon.update(1 / 30); g.input.endFrame(); } g.player.pitch = 0.15; g.birds.frozen = false; return 'ok'; })()" },
  { "eval": "(() => { const g = game, B = g.birds, p = g.player; const b = B.active.find(x => x.state === 'perched' && x.onGround) || B.active.find(x => x.state === 'perched'); if (!b) return 'nenhum pousado'; const px = b.pos.x + 3, pz = b.pos.z; p.position.set(px, g.terrain.heightAt(px, pz), pz); B.update(1 / 30); return { especie: b.species.name, noChao: b.onGround, estadoDepois: b.state }; })()", "label": "susto com o jogador a 3 m" },
  { "eval": "(() => { const g = game, B = g.birds; for (let i = 0; i < 300; i++) B.update(1 / 30); const before = B.active.filter(b => b.state === 'fleeing').length; const inRange = B.active.filter(b => b.pos.distanceTo(g.player.position) < 80 * b.species.wary).length; B.scare(g.player.position, 80); const after = B.active.filter(b => b.state === 'fleeing').length; return { fugindoAntes: before, noRaio: inRange, fugindoDepois: after }; })()", "label": "susto com disparo" },
  { "eval": "(() => { const g = game, B = g.birds; for (let i = 0; i < 1800; i++) B.update(1 / 30); return { ...__census(), geometrias: g.engine.renderer.info.memory.geometries }; })()", "label": "depois de mais 60 s" },
  { "wait": 2000 },
  { "shot": "s4-04-hip" }
]
