[
  { "wait": 4000 },
  { "eval": "(() => { const g = game, p = g.player, w = g.weapon, inp = g.input, t = g.tools; window.__tick = (n) => { for (let i = 0; i < n; i++) { p.update(1 / 30); w.update(1 / 30); t.update(1 / 30); g.birds.update(1 / 30); g.boars.update(1 / 30); g.deer.update(1 / 30); g.hunting.update(1 / 30, g.engine.camera.position); inp.endFrame(); } }; window.__scope = (on) => { if (on !== w.aimToggled) inp.buttonsPressed |= 4; for (let i = 0; i < 20; i++) { p.update(1 / 30); w.update(1 / 30); t.update(1 / 30); inp.endFrame(); } }; window.__herd = (M, n, R, id, turn, clear) => { if (clear) { for (const b of M.active) b.removable = true; __tick(1); } const V = p.position.constructor; let S = null; const eye = g.engine.camera.position; for (let r = R; r <= R + 60 && !S; r += 10) for (let k = 0; k < 40 && !S; k++) { const a = Math.atan2(-Math.sin(p.yaw), -Math.cos(p.yaw)) + turn + (k % 2 ? 1 : -1) * Math.ceil(k / 2) * 0.12; const x = p.position.x + Math.sin(a) * r, z = p.position.z + Math.cos(a) * r; if (!g.chunks.isClear(x, z, 2.5) || !g.terrain.lakes.dry(x, z, 3)) continue; const tgt = new V(x, g.terrain.heightAt(x, z) + 1, z); const dd = tgt.distanceTo(eye); const dir = tgt.clone().sub(eye).normalize(); if (g.terrain.raycast(eye, dir, dd, 0.5) > dd - 1 && !g.chunks.raycastObstacles(eye, dir, dd - 1).kind && g.terrain.places.raycast(eye, dir, dd) > dd - 1) S = { x, z }; } const herd = []; for (let k = 0; k < n; k++) { const b = M.spawn(new V(S.x + k * 2, 0, S.z + (k % 2) * 2), Math.random() * 6.28, k ? 0.9 : 1.05, k); b.herdId = id; herd.push(b); } for (const b of herd) b.leader = herd[0]; return herd; }; inp.pressed.add('Digit4'); __tick(15); const k = document.querySelector('.hud-ammo .kind'); return { ferramenta: t.current, variante: w.variant, supressor: w.model.suppressor.visible, municao: w.magazine, capacidade: w.capacity, rotulo: k.textContent, rotuloVisivel: !k.hidden }; })()", "label": "tecla 4: rifle de rastreio" },
  { "eval": "(() => { game.player.pitch = 0; __tick(2); return 'olhando o rifle'; })()" },
  { "wait": 1500 },
  { "shot": "s30-01-rastreio" },
  { "eval": "(() => { const g = game, p = g.player; const A = __herd(g.deer, 3, 80, 8001, 0, true); const B = __herd(g.deer, 2, 70, 8002, 1.3, false); window.__A = A; window.__B = B; return { bandoA: A.length, distA: +Math.hypot(A[0].pos.x - p.position.x, A[0].pos.z - p.position.z).toFixed(0), bandoB: B.length, distB: +Math.hypot(B[0].pos.x - p.position.x, B[0].pos.z - p.position.z).toFixed(0) }; })()", "label": "bando A a 80 m à frente, bando B a 70 m de lado" },
  { "eval": "(() => { const g = game, p = g.player, w = g.weapon, inp = g.input, A = __A, T = A[0]; g.deer.frozen = true; const V = p.position.constructor; const pt = T.zonePoint('chest', new V()); const eye = g.engine.camera.position; p.yaw = Math.atan2(-(pt.x - eye.x), -(pt.z - eye.z)); p.pitch = Math.atan2(pt.y - eye.y, Math.hypot(pt.x - eye.x, pt.z - eye.z)); __scope(true); const antes = w.magazine; inp.buttonsPressed |= 1; w.update(1 / 30); inp.endFrame(); const dardoNoAr = g.ballistics.bullets.some((b) => b.active && b.dart && b.after < 0); for (let i = 0; i < 20; i++) g.hunting.update(1 / 60, g.engine.camera.position); const s = g.hunting.lastShot; g.deer.frozen = false; __scope(false); __tick(60); return { naLuneta: true, municaoAntes: antes, municaoDepois: w.magazine, dardoNoAr, tiro: s && { tipo: s.kind, letal: s.lethal, pontos: s.points, dist: +s.distance.toFixed(1) }, alvo: { marcado: T.tagged, vivo: T.alive, vida: T.health, estado: T.state }, aviso: document.querySelector('.hud-kill').textContent, bandoB: __B.map((b) => b.state), placar: g.hunting.score }; })()", "label": "dardo pela arma, com a luneta, no peito do veado A" },
  { "wait": 4000 },
  { "eval": "(() => { const g = game, p = g.player, T = __A[0]; const el = document.querySelector('.hud-compass .pin.tag'); return { bussola: el.textContent.replace(/\\s+/g, ' ').trim(), visivel: +el.style.opacity > 0.05, distanciaReal: +Math.hypot(T.pos.x - p.position.x, T.pos.z - p.position.z).toFixed(0) }; })()", "label": "a marca na bússola" },
  { "eval": "(() => { const g = game, p = g.player, T = __A[0]; p.yaw = Math.atan2(-(T.pos.x - p.position.x), -(T.pos.z - p.position.z)); p.pitch = 0; __tick(2); return 'olhando para o marcado'; })()" },
  { "wait": 1500 },
  { "shot": "s30-02-bussola" },
  { "eval": "(() => { const g = game, p = g.player, T = __A[0]; const fx = -Math.sin(p.yaw), fz = -Math.cos(p.yaw); const x = p.position.x - fx * 400, z = p.position.z - fz * 400; p.position.set(x, g.terrain.heightAt(x, z), z); g.chunks.warmup(p.position); __tick(30); return { marcadoAtivo: g.deer.active.includes(T), marcadoVivo: T.alive, outrosDoBandoA: __A.slice(1).map((b) => g.deer.active.includes(b)), bandoB: __B.map((b) => g.deer.active.includes(b)), distancia: +Math.hypot(T.pos.x - p.position.x, T.pos.z - p.position.z).toFixed(0) }; })()", "label": "jogador a 400 m: o marcado continua, os outros somem" },
  { "wait": 4000 },
  { "eval": "(() => { const g = game, p = g.player, T = __A[0]; const el = document.querySelector('.hud-compass .pin.tag'); return { bussola: el.textContent.replace(/\\s+/g, ' ').trim(), distanciaReal: +Math.hypot(T.pos.x - p.position.x, T.pos.z - p.position.z).toFixed(0) }; })()", "label": "a bússola acompanha de longe" },
  { "eval": "(() => { const g = game, p = g.player, B = g.birds; const sp = B.speciesList.find((s) => !s.water && !s.rare); const V = p.position.constructor; p.pitch = 0; p.update(1 / 30); const cam = g.engine.camera; cam.updateMatrixWorld(); const f = new V(); cam.getWorldDirection(f); const b = B.spawnFlying(sp, cam.position.clone().addScaledVector(f, 15), Math.atan2(f.x, f.z) + Math.PI / 2); B.frozen = true; b.update(0.05, B); const dir = b.pos.clone().sub(cam.position).normalize(); g.hunting.shoot(cam.position.clone(), dir, true); for (let i = 0; i < 20; i++) g.hunting.update(1 / 60, cam.position); const s = g.hunting.lastShot; B.frozen = false; return { tiro: s ? s.kind : 'nada no alcance', estadoDaAve: b.state, aveViva: b.alive }; })()", "label": "dardo mirando um pássaro: passa direto" },
  { "eval": "(() => { const g = game, p = g.player, T = __A[0]; const C = __herd(g.deer, 3, 60, 8003, 0, false); window.__C = C; for (const a of C) g.hunting.tagAnimal(g.deer, a, p.position.clone()); const marcados = [...g.deer.active, ...g.boars.active].filter((a) => a.tagged && a.alive); return { marcados: marcados.length, primeiroPerdeuAMarca: !T.tagged, novosMarcados: C.map((a) => a.tagged) }; })()", "label": "4ª marca: a mais antiga cai" },
  { "eval": "(() => { const g = game, p = g.player, w = g.weapon, inp = g.input; inp.pressed.add('Digit1'); __tick(15); const r = { variante: w.variant, supressor: w.model.suppressor.visible, municaoDoRifle: w.magazine, rotuloVisivel: !document.querySelector('.hud-ammo .kind').hidden }; const C0 = __C[0]; g.deer.frozen = true; const V = p.position.constructor; const eye = g.engine.camera.position.clone(); const tiros = []; let nota = ''; for (let k = 0; k < 3 && C0.alive; k++) { const pt = C0.zonePoint('chest', new V()); g.hunting.shoot(eye, pt.clone().sub(eye).normalize(), false); for (let i = 0; i < 20; i++) g.hunting.update(1 / 60, eye); const s = g.hunting.lastShot; tiros.push(s ? s.kind + (s.lethal ? ' letal +' + s.points : ' ' + (s.zone || '')) : 'nada'); nota = document.querySelector('.hud-note').textContent; } g.deer.frozen = false; __tick(30); return { ...r, tirosDoRifle: tiros, abatido: !C0.alive, aviso: nota, restoDoBandoC: __C.slice(1).map((a) => a.state) }; })()", "label": "tecla 1: rifle comum abate um marcado (e o estampido espanta)" },
  { "wait": 4000 },
  { "eval": "(() => { const g = game; const tags = [...document.querySelectorAll('.hud-compass .pin.tag')].filter((el) => +el.style.opacity > 0.05).map((el) => el.textContent.replace(/\\s+/g, ' ').trim()); g.session.clear(); return { marcasNaBussola: tags }; })()", "label": "a marca do abatido some" }
]
